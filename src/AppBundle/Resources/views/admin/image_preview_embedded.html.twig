{% extends 'SonataAdminBundle:CRUD:base_list_field.html.twig' %}
{% block field%}
    <div>
        {% if object != null %}
            {#<img src="{{ vich_uploader_asset(object.image, 'imageFile', AppBundle\\Entity\\Image) }}" />#}
            <span>Try again</span>
        {% else %}s
            <span>No picture</span>
        {% endif %}
    </div>
{% endblock %}